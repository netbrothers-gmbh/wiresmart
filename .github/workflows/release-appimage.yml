name: Release AppImage

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

env:
  APP_NAME: wiresmart

jobs:
  build-appimage:
    name: Build AppImage (x86_64)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Restore Cargo cache
        uses: actions/cache@v4
        with:
          path: |
            .cargo
            .rustup
            target/x86_64
          key: ${{ runner.os }}-x86_64-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-x86_64-cargo-

      - name: Build and package
        uses: uraimo/run-on-arch-action@f9b26e3a1a408d5fd530d20c17b9f3f4428ff8d9 # v3.1.0
        with:
          arch: none
          distro: none
          base_image: ubuntu:22.04
          githubToken: ${{ github.token }}
          dockerRunArgs: |
            --volume "${PWD}/.cargo:/root/.cargo"
            --volume "${PWD}/.rustup:/root/.rustup"
            --volume "${PWD}/target/x86_64:/target"
          env: |
            GITHUB_REF_NAME: ${{ github.ref_name }}
            APPIMAGE_ARCH: x86_64
            APPIMAGE_SUFFIX: x86_64
            APPIMAGETOOL_URL: https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
            APP_NAME: ${{ env.APP_NAME }}
            CARGO_HOME: /root/.cargo
            RUSTUP_HOME: /root/.rustup
            CARGO_TARGET_DIR: /target
          install: |
            apt-get update -y
            apt-get install -y \
              build-essential \
              ca-certificates \
              curl \
              file \
              pkg-config \
              libasound2-dev \
              libgl1-mesa-dev \
              libssl-dev \
              libwayland-dev \
              libx11-dev \
              libxcursor-dev \
              libxi-dev \
              libxinerama-dev \
              libxkbcommon-dev \
              libxrandr-dev
          run: |
            set -euxo pipefail

            export PATH="$CARGO_HOME/bin:$PATH"

            if [ ! -x "$CARGO_HOME/bin/cargo" ]; then
              curl https://sh.rustup.rs -sSf | sh -s -- -y --profile minimal
            fi
            . "$CARGO_HOME/env"

            cargo build --release --locked

            version="${GITHUB_REF_NAME:-}"
            if [ -z "$version" ]; then
              echo "GITHUB_REF_NAME is not set" >&2
              exit 1
            fi
            version="${version#v}"
            artifact_name="${APP_NAME}-${version}-${APPIMAGE_SUFFIX}.AppImage"

            mkdir -p AppDir/usr/bin
            cp "${CARGO_TARGET_DIR}/release/${APP_NAME}" AppDir/usr/bin/${APP_NAME}
            cp assets/logo.png AppDir/${APP_NAME}.png

            printf '%s\n' \
              '#!/bin/sh' \
              'here="$(dirname "$(readlink -f "$0")")"' \
              'exec "$here/usr/bin/wiresmart" "$@"' \
              > AppDir/AppRun
            chmod +x AppDir/AppRun

            printf '%s\n' \
              '[Desktop Entry]' \
              'Type=Application' \
              'Name=NB WireSmart' \
              'Exec=wiresmart' \
              'Icon=wiresmart' \
              'Categories=Network;Utility;' \
              'Terminal=false' \
              > AppDir/${APP_NAME}.desktop

            curl -L "$APPIMAGETOOL_URL" -o appimagetool-${APPIMAGE_ARCH}.AppImage
            chmod +x appimagetool-${APPIMAGE_ARCH}.AppImage

            # Fail early if the download is not an ELF binary (e.g. HTML error page).
            file appimagetool-${APPIMAGE_ARCH}.AppImage
            ARCH="$APPIMAGE_ARCH" ./appimagetool-${APPIMAGE_ARCH}.AppImage --appimage-extract-and-run AppDir "$artifact_name"
            rm -f appimagetool-${APPIMAGE_ARCH}.AppImage

            sha256sum "$artifact_name" > "${artifact_name}.sha256"

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: appimage-x86_64
          if-no-files-found: error
          path: |
            wiresmart-*.AppImage
            wiresmart-*.AppImage.sha256

  publish-release:
    name: Publish release
    runs-on: ubuntu-latest
    needs: build-appimage
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist

      - name: Attach assets to GitHub release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3.0.0
        with:
          files: |
            dist/**/*.AppImage
            dist/**/*.AppImage.sha256
          fail_on_unmatched_files: true
          generate_release_notes: true